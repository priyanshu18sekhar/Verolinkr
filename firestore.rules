rules_version = '2';
// Firestore security rules: deny direct client access to app data.
// All reads/writes go through Next.js API routes using Firebase Admin SDK.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
